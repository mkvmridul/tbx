# Required
PyMySQL>=1.1        # MySQL driver used by finassist.db

# Optional -- only needed if you want the language model to rewrite the prose.
# The numbers are identical either way, because they come from SQL.
openai>=1.40
